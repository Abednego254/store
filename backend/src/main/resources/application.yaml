server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/classicmodels
    username: root
    password: Ciumbe@254
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

